# LPHunt — Claude Instructions

## Git / Deployment
- **Never `git commit` or `git push` without explicit user approval.** Always show what you plan to commit and wait for a "ship it" or equivalent direct instruction before running any git commands.
